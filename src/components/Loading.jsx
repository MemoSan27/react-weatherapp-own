import ReactLoading from "react-loading";

const Loading = () => {
  return (
    <div>
        <ReactLoading type="bars" color="#E6AE01"
            height={100} width={150} />
    </div>
  )
}

export default Loading
